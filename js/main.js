const pertambahan = function (a, b) {
  return a + b;
};

const perkalian = function (a, b) {
  return a * b;
};

const angka1 = 10;
const angka2 = 10;
const result = (perkalian(pertambahan(angka1, angka2), 2));

console.log(result);
